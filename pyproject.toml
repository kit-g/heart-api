[project]
name = "heart-go"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "boto3>=1.42.9",
    "pillow>=12.1.1",
]

[dependency-groups]
dev = [
    "aws-sam-cli>=1.148.0",
    "pyyaml>=6.0.3",
]
