name: "Deploy Assets Service, prod"

on:
  workflow_dispatch: { }
  push:
    branches: [ main ]
    paths:
      - 'assets/**'
      - 'pyproject.toml'
      - 'uv.lock'
      - '.github/workflows/deploy-assets.yml'
      - '.github/workflows/deploy-assets-prod.yml'

permissions:
  id-token: write
  contents: read

concurrency:
  group: assets-prod-${{ github.ref }}
  cancel-in-progress: false

jobs:
  prod:
    uses: ./.github/workflows/deploy-assets.yml
    with:
      AWS_ROLE: 'arn:aws:iam::922419543441:role/heart-github-actions-deploy-role'
      AWS_REGION: 'ca-central-1'
      FUNCTION_NAME: 'heart-assets'
