name: heart_models
description: "Interfaces and data classes"
version: 1.4.0
publish_to: none

environment:
  sdk: ">=3.13.0"

dependencies:
  uuid: ^4.5.3

dev_dependencies:
  flutter_lints: ^6.0.0
  test: ^1.25.6
  mockito: ^5.6.1
  build_runner: ^2.10.4
