# flutter.yaml builds on lints/recommended, so the two includes only add to
# each other; the shared repo config (last) wins any conflict.
include:
  - package:flutter_lints/flutter.yaml
  - ../../analysis_options.yaml
