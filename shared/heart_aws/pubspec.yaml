name: heart_aws
description: "Hand-rolled AWS clients: SigV4 signing, S3 presigned POST, SQS, SNS, Scheduler"
version: 1.0.1
publish_to: none

environment:
  sdk: ">=3.13.0"

dependencies:
  aws_common: ^0.7.13
  aws_signature_v4: ^0.6.10
  crypto: ^3.0.6

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
