name: heart_aws
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: ^3.11.4

dependencies:
  aws_common: ^0.7.13
  aws_signature_v4: ^0.6.10
  crypto: ^3.0.6

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
