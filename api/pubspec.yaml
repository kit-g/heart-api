name: heart
description: "Heart of Yours API"
version: 1.0.0
repository: "https://github.com/kit-g/heart-api.git"
publish_to: none

environment:
  sdk: ">=3.13.0"

dependencies:
  crypto: ^3.0.6
  heart_aws:
    path: "../shared/heart_aws"
  heart_models:
    path: "../shared/heart_models"
    version: 1.8.0
  logging: ^1.3.0
  mime: ^2.0.0
  openid_client: ^0.4.10+1
  postgres: ^3.5.12
  relic: ^1.2.0
  version: ^3.0.2

dev_dependencies:
  analyzer: ^14.1.0
  build_runner: ^2.16.0
  lints: ^6.1.0
  mockito: ^5.8.1
  relic_core: ^1.2.0
  test: ^1.31.2
  aws_common: any
  aws_signature_v4: any
