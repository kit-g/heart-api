# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Code coverage profiles and other test artifacts
*.out
coverage.*
*.coverprofile
profile.cov

# Go workspace file
go.work
go.work.sum

# env file
.env

# Editor/IDE
.idea/
vscode/
.aws-sam/
ignored/
.junie/
.ai/
build/

infrastructure/api/api.toml
content/assets/**

**/.DS_Store

# Python
*.pyc
__pycache__/
.venv/
.pytest_cache/
.ruff_cache/
# Agents (see agents/README.md): worktrees, and the handoff notes plus their
# review (review material, never committed). Root-anchored: on a
# case-insensitive filesystem an unanchored HANDOFF.md also swallows
# docs/handoff.md.
/.claude/worktrees
/HANDOFF.md
/REVIEW.md
